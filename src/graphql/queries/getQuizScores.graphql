query getQuizScores($quizId: String = "") {
  getQuizScores(quizId: $quizId) {
    ... on QueryGetQuizScoresSuccess {
      __typename
      data {
        score
        teamId
        team {
          name
        }
      }
    }
  }
}
