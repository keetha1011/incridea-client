query GetAllSubmissions($day: DayType!) {
  getAllSubmissions(day: $day) {
    ... on QueryGetAllSubmissionsSuccess {
      __typename
      data {
        userId
        user {
          name
          id
        }
        image
        cardId
        card {
          clue
          id
          day
        }
      }
    }
  }
}
